# Release BOM

`AeroSense_Reader_BOM.csv` is published here only after `scripts/check_hardware_bom.py` passes.

The current `pcb v2/NTHU_TEST_BOARD_BOM.docx` is a draft. See `../BOM_SOURCE_AUDIT.md`. Do not link that DOCX as the final BOM.
